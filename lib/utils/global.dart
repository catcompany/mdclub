class Global {
  static Global instance = Global();
  // server address
  static const host = "https://bbs.catcompany.cn";
  // website info
  static const websiteInfo = host + "/api/forum";
  //login
  static const login = host + "/api/login";
}
